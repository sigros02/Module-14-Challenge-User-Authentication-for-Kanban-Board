import { UserLogin } from "../interfaces/UserLogin";

const login = async (_userInfo: UserLogin) => {
  const data = { token: "" }; // SG: adding to get initial code to build
  return data;
};

export { login };
